apiVersion: v1
kind: Service

metadata:
  name: automation-service
  namespace: automation-deployment

spec:
  type: NodePort

  selector:
    app: automation-deployment

  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30080
