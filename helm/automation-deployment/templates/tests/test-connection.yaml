apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "automation-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "automation-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "automation-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
