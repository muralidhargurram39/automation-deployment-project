apiVersion: v1
kind: Service
metadata:
  name: {{ include "automation-deployment.fullname" . }}
  labels:
    {{- include "automation-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "automation-deployment.selectorLabels" . | nindent 4 }}
